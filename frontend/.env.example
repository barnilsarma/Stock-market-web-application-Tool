# Frontend API Configuration

Add this to `frontend/.env.local` if not already present:

```
VITE_API_BASE_URL=http://localhost:3000
```

This ensures the frontend can reach the backend API running on port 3000.
